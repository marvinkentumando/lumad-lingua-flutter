import 'package:flutter/material.dart';

class AdminMapArchitectScreen extends StatelessWidget {
  const AdminMapArchitectScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Feature Removed')),
      body: const Center(
        child: Text('The Map Architect feature has been removed.'),
      ),
    );
  }
}
