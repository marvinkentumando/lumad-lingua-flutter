name: mcp_server_local
description: A local MCP server for Lumad Lingua.
version: 1.0.0
publish_to: none

environment:
  sdk: ^3.5.0

dependencies:
  # No external dependencies used in server.dart yet
